# AI-Python-Django
This repository is used for ai integration for the software.

To run the code and use the AI agent of our software you have to open the file named main.py
then opent the terminal to activate the VE and type: source venv/bin/activate
If you want to run the AI agent in the terminal write : main.py
And if you want to run it with UI write : streamlit run app.py
